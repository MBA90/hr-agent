<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!--
        v1.0.1 — Create JOB_POSTING table
        Author : hr-dev
        Date   : 2025-01-01
    -->

    <changeSet id="v1.0.1-create-job-posting" author="hr-dev">
        <comment>Job vacancy postings managed by HR</comment>

        <createTable tableName="JOB_POSTING">

            <column name="ID" type="NUMBER">
                <constraints primaryKey="true" primaryKeyName="PK_JOB_POSTING" nullable="false"/>
            </column>

            <column name="TITLE" type="VARCHAR2(200)">
                <constraints nullable="false"/>
            </column>

            <column name="DEPARTMENT" type="VARCHAR2(100)"/>

            <column name="DESCRIPTION" type="VARCHAR2(4000)"/>

            <column name="REQUIRED_SKILLS" type="VARCHAR2(2000)">
                <constraints nullable="false"/>
            </column>

            <column name="EXPERIENCE_YEARS" type="NUMBER(3)"/>

            <column name="LOCATION" type="VARCHAR2(100)"/>

            <column name="SALARY_MIN" type="NUMBER(10,2)"/>

            <column name="SALARY_MAX" type="NUMBER(10,2)"/>

            <column name="STATUS" type="VARCHAR2(20)" defaultValue="OPEN">
                <constraints nullable="false"/>
            </column>

            <column name="CREATED_AT" type="TIMESTAMP" defaultValueComputed="SYSTIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="UPDATED_AT" type="TIMESTAMP" defaultValueComputed="SYSTIMESTAMP"/>

        </createTable>

        <!-- Status check constraint -->
        <sql>
            ALTER TABLE JOB_POSTING ADD CONSTRAINT CHK_JOB_STATUS
            CHECK (STATUS IN ('OPEN','CLOSED','ON_HOLD','FILLED'))
        </sql>

        <rollback>
            <dropTable tableName="JOB_POSTING"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
